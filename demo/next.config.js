module.exports = {
  // Uncomment the line below to enable basePath, pages and
  // redirects will then have a path prefix (`/app` in this case)
  //
  // basePath: '/app',module.exports = {
  i18n: {
    locales: ["ar", "en", "fr", "de"],
    defaultLocale: "en",
    localeDetection: false,
  },
};
